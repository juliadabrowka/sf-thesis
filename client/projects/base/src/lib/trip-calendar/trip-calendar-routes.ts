import {Routes} from '@angular/router';
import {TripCalendarComponent} from './trip-calendar.component';

export const tripCalendarRoutes: Routes = [
  {
    path: '',
    component: TripCalendarComponent,
  }
]
